import type { ReactNode } from 'react';
import { NavLink, useNavigate } from 'react-router-dom';
import { Home, MessageCircle, Library, ShieldCheck, LogOut, Sparkles } from 'lucide-react';
import { api } from '../lib/api';
export default function AppShell({children}:{children:ReactNode}){const navigate=useNavigate();const logout=async()=>{await api.logout().catch(()=>{});navigate('/')};const links=[['/dashboard','Home',Home],['/chat','Chat',MessageCircle],['/library','Library',Library],['/parent','Parent',ShieldCheck]] as const;return <div className="app-shell"><aside className="side-nav"><button className="brand" onClick={()=>navigate('/dashboard')}><span className="brand-orb"><Sparkles size={18}/></span><span>Amarktai <b>Kiddo</b></span></button><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>isActive?'nav-link active':'nav-link'}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><button onClick={logout} className="nav-link logout"><LogOut size={19}/><span>Sign out</span></button></aside><main className="main-stage">{children}</main></div>}
