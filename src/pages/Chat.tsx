import { FormEvent,useEffect,useMemo,useState } from 'react';import { useLocation,useNavigate } from 'react-router-dom';import { BookOpen,Image,MessageCircle,Music2,Send,Sparkles } from 'lucide-react';import AppShell from '../components/AppShell';import CompanionAvatar from '../components/CompanionAvatar';import { api } from '../lib/api';import type { Capabilities,Child,Emotion,Message,SessionData } from '../lib/types';
export default function Chat(){const n=useNavigate(),loc=useLocation();const[session,setSession]=useState<SessionData|null>(null),[caps,setCaps]=useState<Capabilities|null>(null),[childId,setChildId]=useState(''),[conversationId,setConversationId]=useState<string|undefined>(new URLSearchParams(loc.search).get('conversation')||undefined),[messages,setMessages]=useState<Message[]>([]),[input,setInput]=useState(''),[mode,setMode]=useState<'chat'|'story'>('chat'),[emotion,setEmotion]=useState<Emotion>('idle'),[busy,setBusy]=useState(false),[error,setError]=useState(''),[createType,setCreateType]=useState<'image'|'audio'|null>(null);useEffect(()=>{void api.lockParent().catch(()=>{});Promise.all([api.me(),api.status()]).then(([me,s])=>{setSession(me);setCaps(s.capabilities);if(me.children[0])setChildId(me.children[0].id)}).catch(()=>n('/login'))},[]);useEffect(()=>{if(conversationId)api.conversation(conversationId).then(r=>{setMessages(r.messages);setChildId(r.conversation.child_id);setMode(r.conversation.mode);const last=[...r.messages].reverse().find(m=>m.role==='assistant'&&m.emotion);if(last?.emotion)setEmotion(last.emotion)}).catch(e=>setError(e.message))},[conversationId]);const child:Child|undefined=useMemo(()=>session?.children.find(c=>c.id===childId),[session,childId]);const send=async(e:FormEvent)=>{e.preventDefault();if(!input.trim()||!childId||busy)return;const value=input.trim();setInput('');setError('');setMessages(m=>[...m,{role:'user',content:value}]);setBusy(true);setEmotion('thinking');try{const r=await api.chat({childId,conversationId,message:value,mode});setConversationId(r.conversationId);setMessages(m=>[...m,{role:'assistant',content:r.reply,emotion:r.emotion}]);setEmotion(r.emotion);if(session)setSession({...session,user:{...session.user,credits:r.credits}})}catch(err:any){setError(err.message);setEmotion('worried')}finally{setBusy(false)}};const create=async(type:'image'|'audio')=>{if(!childId||!input.trim()||busy)return;const value=input.trim();setError('');setCreateType(type);setBusy(true);try{const r=await api.generateMedia({childId,prompt:value,type});setInput('');if(r.media.status==='ready')n('/library');else{setMessages(m=>[...m,{role:'assistant',content:`I started your ${type==='image'?'picture':'music'}! It will appear in the Library when it is ready.`,emotion:'excited'}]);setEmotion('excited')}}catch(err:any){setError(err.message);setEmotion('worried')}finally{setBusy(false);setCreateType(null)}};if(!session)return <AppShell><div className="loading">Loading Kiddo…</div></AppShell>;if(!session.children.length)return <AppShell><div className="empty-state"><CompanionAvatar emotion="curious"/><h2>Add a child profile first</h2><button className="btn primary" onClick={()=>n('/parent')}>Open Parent Controls</button></div></AppShell>;return <AppShell><div className="chat-page"><header className="chat-head"><div><span className="eyebrow">Creative companion</span><h1>{child?.name}'s Kiddo</h1></div><div className="chat-controls"><select value={childId} onChange={e=>{setChildId(e.target.value);setConversationId(undefined);setMessages([])}}>{session.children.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><div className="mode-tabs"><button className={mode==='chat'?'active':''} onClick={()=>setMode('chat')}><MessageCircle/>Chat</button><button className={mode==='story'?'active':''} onClick={()=>setMode('story')}><BookOpen/>Story</button></div><div className="credit-pill small"><Sparkles size={15}/>{session.user.credits}</div></div></header><div className="chat-layout"><aside className="avatar-panel"><CompanionAvatar emotion={emotion} name="Kiddo" speaking={busy}/><p>{busy?'Thinking with you…':emotion==='idle'?'Ready when you are.':`Feeling ${emotion}.`}</p><div className="quick-prompts"><button onClick={()=>setInput('Tell me a funny science fact')}>🧪 Science surprise</button><button onClick={()=>setInput('Let’s invent a magical animal together')}>🦄 Invent something</button><button onClick={()=>{setMode('story');setInput('Tell me an adventure about a brave little explorer')}}>📚 Story idea</button></div></aside><section className="messages"><div className="message-scroll">{!messages.length&&<div className="welcome-bubble"><h2>What should we do today?</h2><p>Chat about anything, invent a story, or type an idea and use the picture/music buttons below.</p></div>}{messages.map((m,i)=><div key={m.id||i} className={`bubble ${m.role}`}><span>{m.content}</span></div>)}{busy&&<div className="bubble assistant typing"><i/><i/><i/></div>}</div>{error&&<div className="notice error">{error}</div>}<form className="composer" onSubmit={send}><textarea value={input} onChange={e=>setInput(e.target.value)} placeholder={mode==='story'?'What should the story be about?':'Say something to Kiddo…'} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();e.currentTarget.form?.requestSubmit()}}}/><div className="composer-actions"><div><button type="button" disabled={!caps?.image||busy} onClick={()=>create('image')} title="Create a picture"><Image/>{createType==='image'?'Creating…':'Picture'}</button><button type="button" disabled={!caps?.music||busy} onClick={()=>create('audio')} title={caps?.music?'Create music':'Music generation needs GenX'}><Music2/>{createType==='audio'?'Creating…':'Music'}</button></div><button className="send-btn" disabled={!input.trim()||busy} type="submit"><Send/></button></div></form></section></div></div></AppShell>}
